姓名：鄒閔旭
學號：C112181141

Q1：請說明管線符號 | 的功能，並舉一個實際使用的例子。
A1：功能：管線符號（Pipe）的作用是**「將前一個指令的標準輸出（stdout），直接當作後一個指令的標準輸入（stdin）」**。它能將多個簡單的指令串聯起來，像生產線一樣接力處理資料。

實際例子：ls -l /etc | less

說明：ls -l /etc 會列出 /etc 目錄下的大量檔案資訊，透過 | 傳給 less 指令後，使用者就可以用鍵盤上下捲動、分頁閱讀這些資訊，而不會被洗板。

Q2：請說明 > 和 >> 的差異，各舉一個使用情境。
A2：> (覆寫 Overwrite)：

差異：將結果寫入檔案。如果檔案已存在，會清空原內容後再寫入新資料；若檔案不存在則建立新檔。

使用情境：建立或重置設定檔。例如：echo "theme=dark" > config.txt（確保檔案裡只有這一行設定）。

>> (附加 Append)：

差異：將結果寫入檔案。如果檔案已存在，會將新資料加在檔案末尾，保留舊有內容。

使用情境：記錄系統日誌（Log）。例如：date >> login_history.txt（每次登入都增加一行時間紀錄，不會刪除之前的紀錄）。

Q3：請寫出一行指令，找出 /etc/passwd 中有多少個使用者的 shell 是 /bin/bash。
    提示：可以結合 grep 和 wc 指令。
A3：grep "/bin/bash" /etc/passwd | wc -l
