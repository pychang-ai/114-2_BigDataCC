姓名:邱奕豪
學號:C112181135

ls -l 列出詳細檔案清單，透過 | 傳給 less 指令，讓你可以分頁捲動查看內容。
ls -l | less

>:覆蓋 (Overwrite) 若檔案已存在，會清空內容再寫入；若不存在則建立新檔。
>>:累加 (Append) 在原有的檔案內容末端接著寫入，不會刪除舊資料。

grep "/bin/bash" /etc/passwd | wc -l