姓名：徐詩雅
學號：C112181116

Q1：請說明管線符號 | 的功能，並舉一個實際使用的例子。
A1：
    將前一個指令的輸出，當作下一個指令的輸入。
        ls -l | grep ".txt"
        (篩選出 .txt 檔案)

Q2：請說明 > 和 >> 的差異，各舉一個使用情境。
A2：
    >：覆蓋寫入（會清空原檔案）
        echo "Hello" > file.txt
        (建立或覆蓋檔案)
    >>：追加寫入（保留原內容）
        echo "Hello" >> file.txt
        (紀錄 log、累積資料)

Q3：請寫出一行指令，找出 /etc/passwd 中有多少個使用者的 shell 是 /bin/bash。
    提示：可以結合 grep 和 wc 指令。
A3：
    grep "/bin/bash" /etc/passwd | wc -l
    (計算使用 /bin/bash 的使用者數量)